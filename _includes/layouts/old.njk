---
layout: layouts/base.njk
---
{# Only include the syntax highlighter CSS on blog posts #}
{%- css %}{% include "node_modules/prismjs/themes/prism-okaidia.css" %}{% endcss %}
{%- css %}{% include "public/css/prism-diff.css" %}{%- endcss %}
<h1>{{ title }}</h1>
{%- css %}{% include "public/css/message-box.css" %}{% endcss %}
<div class="message-box">
	<b><span style="padding:5px; height:30px; text-align:center;width:30px; border-radius:100%; display:inline-block; background:rgba(255,0,0,0.8); user-select:none;margin-right:20px;">!</span> Old Post</b><br>
	<p>You are viewing an older post. Information may not be up-to date and should not be relied on</p>
</div>
<ul class="post-metadata">
	<li>Originally posted on: <time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time></li>
</ul>

{{ content | safe }}

{%- if collections.posts %}
{%- set previousPost = collections.posts | getPreviousCollectionItem %}
{%- set nextPost = collections.posts | getNextCollectionItem %}
{%- if nextPost or previousPost %}
<ul class="links-nextprev">
	{%- if previousPost %}<li>Previous: <a href="{{ previousPost.url }}">{{ previousPost.data.title }}</a></li>{% endif %}
	{%- if nextPost %}<li>Next: <a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a></li>{% endif %}
</ul>
{%- endif %}
{%- endif %}
